package com.apiserver;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCodeResourceIT extends CodeResourceTest {

    // Execute the same tests but in native mode.
}